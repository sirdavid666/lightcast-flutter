plugins {
    id "com.android.application"
    id "org.jetbrains.kotlin.android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.lightcast.camera"
    compileSdk = 36 // Changed from flutter.compileSdkVersion to fix flutter_webrtc error

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "com.lightcast.camera"
        minSdk = 24
        targetSdk = 36 // Bumped to 36 to match compileSdk and prevent runtime warnings
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }
}

flutter {
    source = "../.."
}
